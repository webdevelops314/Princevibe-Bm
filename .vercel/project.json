{
  "projectId": "princevibe-business-manager",
  "orgId": "your-org-id",
  "settings": {
    "framework": "create-react-app",
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "client/build",
    "installCommand": "npm install",
    "rootDirectory": "client"
  }
}
